let allSchemes=[];function renderSchemeCard(s){const score=s.score,color=score.total_score>=70?'badge-green':score.total_score>=40?'badge-orange':'badge-red';return `<div class="card glass"><div style="display:flex;justify-content:space-between;align-items:start;"><h3>${s.name}</h3><span class="badge ${color}">${score.total_score}% match</span></div><p style="color:var(--text-muted);margin:8px 0;">${s.description}</p><span class="badge badge-blue">${s.category}</span><div class="progress-bar" style="margin-top:12px;"><div class="progress-fill" style="width:${score.total_score}%;"></div></div><p style="font-size:.8rem;margin-top:6px;color:var(--text-muted);">Eligibility ${score.eligibility_score}/60 · Docs ${score.document_score}/30 · Profile ${score.completeness_score}/10</p>${score.missing_documents.length?`<p style="font-size:.8rem;color:#9c4535;">Missing docs: ${score.missing_documents.join(', ')}</p>`:''}<div style="display:flex;gap:10px;margin-top:14px;flex-wrap:wrap;"><button class="btn btn-outline" onclick="saveScheme(${s.id})">Save</button><a class="btn btn-primary" href="${s.official_url}" target="_blank">Apply now</a><button class="btn btn-secondary" onclick="createApplication('scheme',${s.id},'${s.name.replace(/'/g,"\\'")}')">Track application</button></div></div>`}async function loadSchemes(){const r=await apiFetch('/api/schemes/recommended');if(!r||!r.ok)return;allSchemes=await r.json();renderSchemes(allSchemes)}function renderSchemes(s){document.getElementById('schemesGrid').innerHTML=s.map(renderSchemeCard).join('')||'<p>No schemes found.</p>'}function filterSchemes(){const c=document.getElementById('categoryFilter').value,min=parseInt(document.getElementById('scoreFilter').value||'0');renderSchemes(allSchemes.filter(s=>(!c||s.category===c)&&s.score.total_score>=min))}async function saveScheme(id){await apiFetch(`/api/schemes/${id}/save`,{method:'POST'});alert('Scheme saved.')}async function createApplication(type,refId,name){const r=await apiFetch('/api/applications',{method:'POST',body:JSON.stringify({type,ref_id:refId})});if(r&&r.ok)window.location.href='/applications'}document.addEventListener('DOMContentLoaded',()=>{loadSchemes();document.getElementById('categoryFilter')?.addEventListener('change',filterSchemes);document.getElementById('scoreFilter')?.addEventListener('input',filterSchemes)});
